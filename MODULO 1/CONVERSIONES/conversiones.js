// FUNCTION CONVERTIR LITROS A CENTILITROS 
function conv_Litros_Cent (litros){
  let centiLitros = litros*100;
  console.log(litros + " litros son "+ centiLitros + " centilitros");
}
conv_Litros_Cent(3);
// FUNCTION CONVERTIR LITROS A DECILITROS
function conv_Litros_Deci (litros){
    let deciLitros = litros*10;
    console.log(litros + " litros son "+ deciLitros + " decilitros");
  }
  
  conv_Litros_Deci(3);

  // FUNCTION CONVERTIR LITROS A GALONES
function conv_Litros_Galon (litros){
  let galon = litros * 0.264172;
  console.log(litros + " litros son "+ galon + " Galones");
}

conv_Litros_Galon(3);

// FUNCTION CONVERTIR LITROS A ONZAS
function conv_Litros_Onza (litros){
  let onza = litros * 33.814;
  console.log(litros + " litros son "+ onza + " Onzas");
}

conv_Litros_Onza(3);